# Frontend (Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_AUTH_MODE=mock

# Serverless (Vercel / API routes)
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
RESEND_FROM_EMAIL=itsm@petacorp.com.br
RESEND_WEBHOOK_SECRET=

# Google Gemini (já usado em /api/chat)
API_KEY=

# Public app URL (used by backend to build reset/setup links)
APP_URL=

# Optional token hashing secret (recommended)
RESET_TOKEN_SECRET=

# Optional CORS allowlist (comma-separated). If empty, only APP_URL is allowed when Origin is present.
CORS_ALLOWED_ORIGINS=

# Enable legacy general-purpose email endpoint (/api/send-email)
ENABLE_SEND_EMAIL_ENDPOINT=false
